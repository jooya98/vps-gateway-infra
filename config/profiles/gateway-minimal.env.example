# Minimal gateway profile. SOCKS is local-only unless firewall policy is
# explicitly changed; no Docker, monitoring, or other optional services run.
PROFILE_NAME=gateway-minimal
SOCKS_LISTEN_ADDRESS=127.0.0.1
SOCKS_LISTEN_PORT=1080
SING_BOX_LOG_LEVEL=warn
