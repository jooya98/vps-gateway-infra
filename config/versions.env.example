# Official upstream release versions.
# latest resolves the current stable release at deployment time and is not
# byte-for-byte reproducible. Set explicit versions for reproducible rebuilds.
SING_BOX_VERSION=latest
CLOUDFLARED_VERSION=latest
